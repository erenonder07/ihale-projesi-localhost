<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>İlanı Düzenle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { 
            font-family: 'Inter', sans-serif; 
            background-color: #f8f9fa; 
        }

        /* --- LOGO --- */
        .navbar-brand { 
            font-family: 'Outfit', sans-serif; 
            display: flex; align-items: center; gap: 12px; 
            text-decoration: none; 
        }
        .brand-icon-box { 
            width: 40px; height: 40px; 
            background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%); 
            color: white; border-radius: 10px; 
            display: flex; align-items: center; justify-content: center; 
            font-size: 1.1rem; 
        }
        .brand-text-main { font-weight: 800; font-size: 1.25rem; color: #212529; }
        
        /* Editör Yüksekliği */
        .ck-editor__editable_inline {
            min-height: 250px;
        }

        /* Kart ve Form */
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.04);
        }
        .form-label {
            font-weight: 600;
            color: #495057;
            font-size: 0.9rem;
        }
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg bg-white border-bottom py-3 mb-5">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <div class="brand-icon-box"><i class="fas fa-gavel"></i></div>
                <span class="brand-text-main">İHALE SİSTEMİ</span>
            </a>
            <a href="/my-tenders" class="btn btn-light border btn-sm px-3 fw-bold">
                <i class="fas fa-times me-1"></i> İptal
            </a>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                
                <div class="card p-4">
                    <div class="border-bottom pb-3 mb-4">
                        <h4 class="fw-bold m-0"><i class="fas fa-edit text-primary me-2"></i> İlanı Düzenle</h4>
                    </div>

                    <form action="/duzenle/<%= tender.tender_id %>" method="post" enctype="multipart/form-data">
                        
                        <div class="mb-3">
                            <label class="form-label">İlan Başlığı</label>
                            <input type="text" name="title" class="form-control" value="<%= tender.title %>" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">İlan Görseli</label>
                            <div class="d-flex align-items-center gap-3">
                                <% if(tender.image_url) { %>
                                    <img src="/static/images/<%= tender.image_url %>" class="rounded border" width="60" height="60" style="object-fit: cover;">
                                <% } else { %>
                                    <div class="bg-light rounded border d-flex align-items-center justify-content-center text-muted" style="width: 60px; height: 60px;">
                                        <i class="fas fa-image"></i>
                                    </div>
                                <% } %>
                                
                                <input type="file" name="image" class="form-control">
                            </div>
                            <div class="form-text small text-muted">Resmi değiştirmek istemiyorsanız boş bırakın. Yeni resim yüklerseniz eskisi silinir.</div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Açıklama</label>
                            <textarea id="editor" name="description"><%= tender.description %></textarea>
                        </div>

                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label">Başlangıç Fiyatı</label>
                                <div class="input-group">
                                    <input type="number" name="start_price" class="form-control" value="<%= tender.start_price %>" required>
                                    <span class="input-group-text bg-light">₺</span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Bitiş Tarihi</label>
                                <% 
                                    let date = new Date(tender.end_date);
                                    date.setMinutes(date.getMinutes() - date.getTimezoneOffset());
                                    let dateStr = date.toISOString().slice(0, 16); 
                                %>
                                <input type="datetime-local" name="end_date" class="form-control" value="<%= dateStr %>" required>
                            </div>
                        </div>

                        <div class="d-flex justify-content-end gap-2">
                            <a href="/my-tenders" class="btn btn-outline-secondary px-4">Vazgeç</a>
                            <button type="submit" class="btn btn-primary px-4 fw-bold">
                                <i class="fas fa-save me-2"></i> Değişiklikleri Kaydet
                            </button>
                        </div>
                    </form>

                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create( document.querySelector( '#editor' ), {
                toolbar: [ 'heading', '|', 'bold', 'italic', 'link', 'bulletedList', 'numberedList', 'blockQuote', '|', 'undo', 'redo' ]
            } )
            .catch( error => { console.error( error ); } );
    </script>

</body>
</html>